/** Standard field focus — use with `.form-focus-safe` parent so the ring is not clipped. */
export const FORM_CONTROL_FOCUS =
  "focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring";

/** Slight horizontal gutter so outer focus rings are not clipped by overflow. */
export const FORM_FOCUS_SAFE = "px-0.5 -mx-0.5";
